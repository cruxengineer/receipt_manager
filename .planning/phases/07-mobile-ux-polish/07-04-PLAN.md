---
phase: 07-mobile-ux-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "Full receipt split flow (capture → swipe → summary) completable in under 2 minutes on iPhone Safari"
    - "No content clipped behind Dynamic Island or home indicator on iPhone 16"
    - "No tap highlight flash on any button or interactive element"
    - "Screen-to-screen slide transitions feel smooth and natural"
    - "All touch targets are comfortably tappable without precise aiming"
    - "App does not require login — sessions work without any credentials"
  artifacts:
    - path: ".planning/phases/07-mobile-ux-polish/07-04-SUMMARY.md"
      provides: "Human verification record with pass/fail results"
  key_links:
    - from: "iPhone Safari live URL"
      to: "Human tester"
      via: "Manual test of the complete flow"
      pattern: "N/A — human verification"
---

<objective>
Human verification of all Phase 7 mobile UX changes on iPhone Safari. The tester runs through the complete receipt split flow and confirms that all iOS polish changes work correctly on the target device.

Purpose: CSS viewport fixes, safe area insets, and touch behaviors can only be confirmed on a real iPhone. Automated tests cannot verify visual rendering or physical gesture feel. This checkpoint gates Phase 7 completion.
Output: Human confirmation that all 4 UX requirements are met on iPhone Safari.
</objective>

<execution_context>
@C:/Users/Ethan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ethan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-mobile-ux-polish/07-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: iPhone Safari verification of all Phase 7 UX changes</name>
  <files>none</files>
  <action>No automated action — this is a human verification checkpoint. Run `npm run dev`, open on iPhone 16 Safari, and work through the verification steps below.</action>
  <verify>
    <automated>npm test 2>&1 | tail -3</automated>
  </verify>
  <done>All 19 verification steps confirmed by human tester on iPhone 16 Safari. Tester types "approved".</done>
  <what-built>
    Plan 07-01: Global iOS Safari polish — viewport-fit=cover, tap highlight removal, double-tap zoom prevention, overscroll-behavior: none, CaptureScreen and SummaryScreen switched to h-dvh layout with safe area insets.

    Plan 07-02: Touch target expansion — SwipeScreen back arrow and three-dots menu expanded to 44x44px, SummaryScreen pencil edit icon expanded to 44x44px.

    Plan 07-03: Screen-to-screen slide transitions — forward navigation slides in from right (350ms), Adjust slides back (screen exits right), Start Over fades out.
  </what-built>
  <how-to-verify>
    Run the dev server (`npm run dev`) and open the local URL on your iPhone 16 in Safari. Or use the deployed Vercel URL if already deployed.

    Work through these checks in order:

    **Safe area insets (Dynamic Island + home indicator)**
    1. Open the app — confirm the PasswordGate content is not hidden behind the Dynamic Island notch at the top
    2. On the CaptureScreen, confirm the "ReceiptSplit" header is fully visible below the Dynamic Island
    3. On the SwipeScreen, confirm the Adjust/Split buttons at the bottom are not obscured by the home indicator bar
    4. On the SummaryScreen, confirm the Adjust / Start Over buttons at the bottom clear the home indicator bar

    **No tap highlight flash**
    5. Tap the "Process Receipt" button (or any button) — confirm no blue/grey flash appears on tap
    6. Tap the Split circle button in SwipeScreen — confirm no tap highlight

    **No double-tap zoom**
    7. Double-tap the price number on a swipe card — confirm the viewport does NOT zoom in

    **Scroll behavior**
    8. On the SummaryScreen with several items, scroll the item list — confirm no rubber-band overscroll past the content edges
    9. On the CaptureScreen with processing state, confirm the screen does not scroll beyond the viewport

    **Touch targets**
    10. In SwipeScreen, tap the back arrow (upper left) — confirm it responds reliably without precise aiming
    11. In SwipeScreen, tap the three-dots menu (upper right) — confirm it responds reliably
    12. In SummaryScreen, tap the pencil icon next to a person's name — confirm it opens the name edit modal reliably

    **Screen transitions**
    13. From CaptureScreen, upload a receipt and tap Process — confirm the ReviewScreen slides in from the right
    14. On ReviewScreen, tap Start Splitting — confirm SwipeScreen slides in from the right
    15. After assigning all items, confirm the Summary slides in from the right (after the 1.5s All Done pause)
    16. On SummaryScreen, tap Adjust — confirm the SwipeScreen slides in from the LEFT (back navigation feel)
    17. On SummaryScreen, tap Start Over — confirm a brief fade-out before returning to CaptureScreen

    **No login required**
    18. Close and reopen the app tab — confirm the API key gate only appears if sessionStorage was cleared (session persists within the same tab/session)
    19. Confirm there are no user account prompts or mandatory sign-up screens
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe any issues found (e.g., "safe area not working on step 2 — header behind notch").</resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. All 19 checks above must pass on iPhone Safari for Phase 7 to be complete.
</verification>

<success_criteria>
1. All 19 verification steps pass on iPhone 16 Safari
2. UX-01: App works smoothly on iPhone Safari with no layout glitches
3. UX-02: Back arrow, three-dots, and pencil icons respond without precise aiming
4. UX-03: Full split flow completable comfortably in under 2 minutes
5. UX-04: No login required (PasswordGate is a one-time API key entry, not user auth — confirmed by tester)
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-ux-polish/07-04-SUMMARY.md`
</output>
