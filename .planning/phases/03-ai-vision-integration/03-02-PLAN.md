---
phase: 03-ai-vision-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gate/PasswordGate.tsx
  - src/components/gate/PasswordGate.test.tsx
autonomous: true
requirements:
  - AI-01

must_haves:
  truths:
    - "User sees a centered passphrase entry screen before any other content"
    - "Entering the correct passphrase (matching VITE_APP_PASSWORD) calls onUnlock"
    - "Entering an incorrect passphrase shows an error message"
    - "The input type is password (masked) and submit works via Enter key or button tap"
    - "The component renders nothing UI-blocking — it is a simple centered card"
  artifacts:
    - path: "src/components/gate/PasswordGate.tsx"
      provides: "Passphrase entry screen component"
      exports: ["PasswordGate"]
    - path: "src/components/gate/PasswordGate.test.tsx"
      provides: "Automated tests for gate behavior"
  key_links:
    - from: "src/components/gate/PasswordGate.tsx"
      to: "import.meta.env.VITE_APP_PASSWORD"
      via: "env var comparison in onSubmit handler"
      pattern: "VITE_APP_PASSWORD"
    - from: "src/App.tsx"
      to: "src/components/gate/PasswordGate.tsx"
      via: "rendered when appState === 'gate'"
      pattern: "appState.*gate"
---

<objective>
Build the PasswordGate component — a simple passphrase screen that blocks access until the correct VITE_APP_PASSWORD is entered.

Purpose: Protects the client-side API key from casual discovery. Lightweight — more like a PIN entry than a login page. App.tsx (Plan 04) will render this when sessionStorage does not contain a valid auth flag.

Output: src/components/gate/PasswordGate.tsx, with tests covering correct/incorrect passphrase behavior.
</objective>

<execution_context>
@C:/Users/Ethan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ethan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-vision-integration/03-CONTEXT.md
@src/components/ui/button.tsx
@src/lib/utils.ts
</context>

<interfaces>
<!-- PasswordGate is self-contained. App.tsx (Plan 04) will import it with this API. -->

From src/components/gate/PasswordGate.tsx (CREATE THIS FILE):
```typescript
interface PasswordGateProps {
  /** Called when user enters the correct passphrase. App.tsx advances state to 'capture'. */
  onUnlock: () => void
}

export function PasswordGate({ onUnlock }: PasswordGateProps): JSX.Element
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PasswordGate component with tests</name>
  <files>src/components/gate/PasswordGate.tsx, src/components/gate/PasswordGate.test.tsx</files>
  <behavior>
    - Test 1 (renders): PasswordGate renders a password input and a submit button
    - Test 2 (correct password): Typing the value of import.meta.env.VITE_APP_PASSWORD and clicking submit calls onUnlock once
    - Test 3 (wrong password): Typing an incorrect value and clicking submit does NOT call onUnlock and shows an error message containing "Incorrect" (case-insensitive)
    - Test 4 (Enter key): Pressing Enter in the password input with correct value calls onUnlock
    - Test 5 (empty submit): Clicking submit with empty input does NOT call onUnlock and shows an error message
    - Test 6 (input type): The input element has type="password" (masked)
  </behavior>
  <action>
    RED phase: Write src/components/gate/PasswordGate.test.tsx. In the test file, mock import.meta.env.VITE_APP_PASSWORD as 'test-secret'. Write all 6 tests. Run tests — all must fail (component does not exist yet).

    GREEN phase: Implement src/components/gate/PasswordGate.tsx:

    Layout: fullscreen min-h-screen bg-gray-50, centered card max-w-md mx-auto, same established pattern as CaptureScreen.

    Behavior:
    - Controlled input state for the passphrase value
    - onSubmit handler: compare trimmed input value against import.meta.env.VITE_APP_PASSWORD
      - Match → call onUnlock()
      - No match (including empty) → set error state to "Incorrect passphrase."
    - Form submission via both button click and Enter key (use <form onSubmit={...}>)
    - Error message rendered below input when error state is non-null
    - Input autofocus on mount (autoFocus attribute)

    UI copy (use Claude's discretion on exact wording, keep it minimal):
    - Heading: "ReceiptSplit"
    - Subheading: "Enter passphrase to continue"
    - Button: "Unlock"
    - Error: "Incorrect passphrase."

    Use shadcn/ui Button from @/components/ui/button for the submit button.
    Use cn() from @/lib/utils for class merging.
    Tailwind v4 CSS-first — no tailwind.config.js.
    Follow max-w-md mx-auto card layout established in CaptureScreen.
  </action>
  <verify>
    <automated>npx vitest run src/components/gate/PasswordGate.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 6 tests GREEN. PasswordGate renders, validates correct/incorrect passphrase, handles Enter key, shows error message.</done>
</task>

</tasks>

<verification>
- npx vitest run src/components/gate/ — all tests pass
- TypeScript: npx tsc --noEmit — no errors
- PasswordGate renders: password input, Unlock button, centered card layout
- Correct passphrase → onUnlock called
- Incorrect passphrase → error shown, onUnlock NOT called
</verification>

<success_criteria>
- All 6 PasswordGate tests GREEN
- TypeScript clean
- Component matches established max-w-md centered card pattern
- Uses shadcn/ui Button and cn() from lib/utils
- No additional state management — purely controlled via onUnlock prop
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-vision-integration/03-02-SUMMARY.md`
</output>
