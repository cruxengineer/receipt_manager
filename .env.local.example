# Anthropic API key for Claude vision (required for receipt scanning)
VITE_ANTHROPIC_API_KEY=your_api_key_here

# Mock mode for testing without API calls (set to true to use sample receipt data)
VITE_MOCK_MODE=false
