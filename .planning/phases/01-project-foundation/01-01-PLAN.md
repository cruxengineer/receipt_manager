---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - .env.local.example
  - .gitignore
autonomous: true
requirements:
  - DEPL-01

must_haves:
  truths:
    - Dev server runs on localhost without errors
    - TypeScript compilation succeeds
    - Tailwind utility classes apply styling correctly
    - Environment variables load from .env.local
  artifacts:
    - path: package.json
      provides: Project dependencies and scripts
      contains: "vite"
    - path: vite.config.ts
      provides: Build configuration
      exports: ["default"]
    - path: src/main.tsx
      provides: React application entry point
      min_lines: 5
    - path: tailwind.config.js
      provides: Tailwind CSS configuration
      contains: "content"
  key_links:
    - from: index.html
      to: src/main.tsx
      via: script module tag
      pattern: '<script.*src=".*main\.tsx"'
    - from: src/main.tsx
      to: src/App.tsx
      via: React component import
      pattern: 'import.*App.*from'
    - from: src/index.css
      to: tailwind.config.js
      via: Tailwind directives
      pattern: '@tailwind'
---

<objective>
Initialize Vite + React + TypeScript project with Tailwind CSS and establish core build configuration for a mobile-first static web app.

Purpose: Create the foundational project structure with modern build tooling, type safety, and utility-first styling that will support the entire ReceiptSplit application.

Output: Working development environment with hot reload, TypeScript compilation, Tailwind styling, and environment variable support.
</objective>

<execution_context>
@C:/Users/Ethan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ethan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Ethan/OneDrive/Documents/Receipt_Manager/.planning/PROJECT.md
@C:/Users/Ethan/OneDrive/Documents/Receipt_Manager/.planning/ROADMAP.md
@C:/Users/Ethan/OneDrive/Documents/Receipt_Manager/.planning/STATE.md
@C:/Users/Ethan/OneDrive/Documents/Receipt_Manager/.planning/phases/01-project-foundation/01-CONTEXT.md

## User Decisions from CONTEXT.md

**Locked decisions:**
- Framework: React + TypeScript
- Build tool: Vite
- CSS framework: Tailwind CSS (utility-first, mobile-optimized)
- Environment variables: `.env.local` for API keys (ANTHROPIC_API_KEY, VITE_MOCK_MODE)

**Project type:** Mobile-first static web app (client-side only, no backend)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Vite React TypeScript project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.node.json
    index.html
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
  </files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` to scaffold the project in the current directory. Accept defaults and overwrite prompt if needed.

    After scaffold:
    1. Install dependencies: `npm install`
    2. Verify the default Vite + React + TypeScript setup works
    3. Keep default project structure: src/, public/, index.html

    Do NOT customize App.tsx yet - that happens in Plan 02. This task establishes the build system only.
  </action>
  <verify>
    <automated>npm run dev -- --port 5173 & sleep 5 && curl http://localhost:5173 | grep -q "Vite" && kill %1</automated>
  </verify>
  <done>
    Vite dev server starts without errors, TypeScript compiles successfully, default React app renders in browser at localhost:5173
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure Tailwind CSS</name>
  <files>
    tailwind.config.js
    postcss.config.js
    src/index.css
  </files>
  <action>
    Install Tailwind CSS and configure for Vite:

    1. Install packages: `npm install -D tailwindcss postcss autoprefixer`
    2. Initialize Tailwind: `npx tailwindcss init -p`
    3. Configure tailwind.config.js content paths:
       ```js
       content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
       ```
    4. Replace src/index.css contents with Tailwind directives:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
    5. Verify index.css is imported in src/main.tsx (Vite template should include this)

    Mobile-first strategy: Tailwind is mobile-first by default (breakpoints are min-width). No additional configuration needed yet.
  </action>
  <verify>
    <automated>grep -q "@tailwind base" src/index.css && grep -q "content:" tailwind.config.js</automated>
  </verify>
  <done>
    Tailwind CSS installed and configured, utility classes available for use, PostCSS processing enabled in Vite build pipeline
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Set up environment variables and gitignore</name>
  <files>
    .env.local.example
    .gitignore
  </files>
  <action>
    Create environment variable structure for API keys and feature flags:

    1. Create .env.local.example with:
       ```
       # Anthropic API key for Claude vision
       VITE_ANTHROPIC_API_KEY=your_api_key_here

       # Mock mode for testing without API calls
       VITE_MOCK_MODE=false
       ```

    2. Update .gitignore to ensure .env.local is excluded (Vite template should include this, but verify):
       - Confirm `.env.local` is listed in .gitignore
       - If not present, add `.env*.local` pattern

    3. Do NOT create actual .env.local file yet - user will add real API key later

    Note: Using VITE_ prefix for environment variables per Vite requirements (exposes to client-side code).
  </action>
  <verify>
    <automated>test -f .env.local.example && grep -q "VITE_ANTHROPIC_API_KEY" .env.local.example && grep -q "\.env.*\.local" .gitignore</automated>
  </verify>
  <done>
    .env.local.example file created with API key and mock mode placeholders, .gitignore protects actual .env.local from git commits
  </done>
</task>

</tasks>

<verification>
Run complete development workflow:
1. `npm run dev` starts server without errors
2. Browser at localhost:5173 shows Vite + React default page
3. TypeScript compilation has zero errors
4. Tailwind utilities can be applied (test by adding className="text-blue-500" to App.tsx temporarily)
5. .env.local.example exists and documents required environment variables
</verification>

<success_criteria>
- [ ] Vite development server runs successfully on localhost
- [ ] TypeScript compilation completes without errors
- [ ] Tailwind CSS utility classes work when applied to components
- [ ] Environment variable template created and gitignore configured
- [ ] Project structure follows Vite + React conventions (src/, public/, index.html)
- [ ] All dependencies installed and locked in package-lock.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
