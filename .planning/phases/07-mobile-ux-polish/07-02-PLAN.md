---
phase: 07-mobile-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/swipe/SwipeScreen.tsx
  - src/components/summary/SummaryScreen.tsx
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Back arrow button in SwipeScreen has at least 44x44px tap area"
    - "Three-dots menu button in SwipeScreen has at least 44x44px tap area"
    - "Pencil edit icon in SummaryScreen has at least 44x44px tap area"
    - "Icon visuals remain small and unchanged — only the invisible tap zone grows"
  artifacts:
    - path: "src/components/swipe/SwipeScreen.tsx"
      provides: "Back and three-dots buttons with expanded hit areas"
      contains: "min-w-[44px] min-h-[44px]"
    - path: "src/components/summary/SummaryScreen.tsx"
      provides: "Pencil edit button with expanded hit area"
      contains: "min-w-[44px] min-h-[44px]"
  key_links:
    - from: "SwipeScreen back button"
      to: "handleBack handler"
      via: "expanded tappable wrapper div"
      pattern: "min-w-.*44px.*onClick"
    - from: "SummaryScreen pencil button"
      to: "onEditNames handler"
      via: "expanded tappable wrapper div"
      pattern: "min-w-.*44px.*onEditNames"
---

<objective>
Expand the invisible tap areas of three small icon buttons to meet the 44x44px minimum touch target requirement: the back arrow and three-dots menu in SwipeScreen, and the pencil edit icon in SummaryScreen. Visual appearance of each icon is unchanged — only the tappable zone grows.

Purpose: UX-02 requires thumb-friendly touch targets. These three icon-only buttons are the only non-compliant elements — all other buttons already use h-11 (44px) via shadcn/ui Button.
Output: SwipeScreen.tsx and SummaryScreen.tsx with corrected button hit areas.
</objective>

<execution_context>
@C:/Users/Ethan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ethan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-mobile-ux-polish/07-CONTEXT.md
@src/components/swipe/SwipeScreen.tsx
@src/components/summary/SummaryScreen.tsx
</context>

<interfaces>
<!-- Touch target expansion pattern — keep icon visually small, grow tappable zone -->
<!-- Technique: add min-w-[44px] min-h-[44px] flex items-center justify-center to the button itself -->
<!-- For shadcn/ui ghost Button components, override the size to ensure 44px minimum -->
<!-- For raw <button> elements, add the classes directly -->

SwipeScreen top bar buttons currently use shadcn/ui Button with size="sm" (h-9, ~36px).
Approach: Add className override `min-w-[44px] min-h-[44px]` to both back and menu Buttons.

SummaryScreen pencil button is a raw <button> with just a Pencil icon inside.
Approach: Add `min-w-[44px] min-h-[44px] flex items-center justify-center` to the button className.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand SwipeScreen back and three-dots button hit areas</name>
  <files>src/components/swipe/SwipeScreen.tsx</files>
  <action>
    In SwipeScreen.tsx, find the top bar section (the div with `flex items-center justify-between px-3 pt-4 pb-1`).

    1. Back arrow Button: add `min-w-[44px] min-h-[44px]` to the existing className. The Button already has `variant="ghost" size="sm"` — just ensure the rendered element meets minimum size. Change:
       ```tsx
       <Button
         variant="ghost"
         size="sm"
         onClick={handleBack}
         disabled={currentIndex === 0 && assignments.length === 0}
         aria-label="back"
       >
         <ArrowLeft className="w-5 h-5" />
       </Button>
       ```
       To:
       ```tsx
       <Button
         variant="ghost"
         size="sm"
         onClick={handleBack}
         disabled={currentIndex === 0 && assignments.length === 0}
         aria-label="back"
         className="min-w-[44px] min-h-[44px]"
       >
         <ArrowLeft className="w-5 h-5" />
       </Button>
       ```

    2. Three-dots menu Button: same change — add `className="min-w-[44px] min-h-[44px]"`. Change:
       ```tsx
       <Button
         variant="ghost"
         size="sm"
         onClick={() => setMenuOpen(v => !v)}
         aria-label="menu"
       >
         <MoreHorizontal className="w-5 h-5" />
       </Button>
       ```
       To:
       ```tsx
       <Button
         variant="ghost"
         size="sm"
         onClick={() => setMenuOpen(v => !v)}
         aria-label="menu"
         className="min-w-[44px] min-h-[44px]"
       >
         <MoreHorizontal className="w-5 h-5" />
       </Button>
       ```

    No other changes to SwipeScreen.tsx. Do not alter gesture logic, card rendering, or totals bar.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both ghost buttons in SwipeScreen top bar have min-w-[44px] min-h-[44px]. Build passes. No other SwipeScreen changes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand SummaryScreen pencil edit button hit area</name>
  <files>src/components/summary/SummaryScreen.tsx</files>
  <action>
    In SummaryScreen.tsx, there are two pencil edit buttons (one for Person A, one for Person B) in the totals bar. Both currently look like:
    ```tsx
    <button
      onClick={onEditNames}
      aria-label="edit names"
      className="opacity-50 hover:opacity-100 transition-opacity"
    >
      <Pencil className="w-3 h-3" />
    </button>
    ```

    Update BOTH pencil buttons to add minimum tap dimensions:
    ```tsx
    <button
      onClick={onEditNames}
      aria-label="edit names"
      className="opacity-50 hover:opacity-100 transition-opacity min-w-[44px] min-h-[44px] flex items-center justify-center"
    >
      <Pencil className="w-3 h-3" />
    </button>
    ```

    The icon `w-3 h-3` stays visually small. The button element itself grows to 44x44px minimum. The `flex items-center justify-center` centers the icon within the larger tap zone.

    Note: This is also the file touched by plan 07-01 (Task 2 adds pt-safe and pb-safe-6). If executing after 07-01, the file will already have those safe area classes — preserve them and only add the hit area changes here.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both pencil buttons in SummaryScreen totals bar have min-w-[44px] min-h-[44px] flex items-center justify-center. Pencil icon remains w-3 h-3 visually. Build passes.</done>
</task>

</tasks>

<verification>
Run `npm run build` — must pass.
Run `npm test` — all 85 existing tests must remain GREEN.
</verification>

<success_criteria>
1. SwipeScreen back button has className including min-w-[44px] min-h-[44px]
2. SwipeScreen three-dots button has className including min-w-[44px] min-h-[44px]
3. Both SummaryScreen pencil buttons have min-w-[44px] min-h-[44px] flex items-center justify-center
4. All icon visuals unchanged (w-3 h-3 pencil, w-5 h-5 arrow/dots)
5. `npm run build` and `npm test` pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-ux-polish/07-02-SUMMARY.md`
</output>
